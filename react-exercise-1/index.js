import getData from "./app.js";

// Importing the getData function from the app.js file.

// Calling the getData function with an argument of 1.
// This will initiate the process of fetching data for the user with ID 1.

getData(1);